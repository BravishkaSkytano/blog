---
layout: page
pagination:
  data: collections
  size: 1
  alias: tag
title: Tagged '{{ tag }}'
permalink: /tags/{{ tag }}/
---

<ul>
  {% set taglist = collections[ tag ] %}
  {% for post in taglist | reverse %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </li>
  {% endfor %}
</ul>
